- Make node type and tax type names the same as Drupal 7 for migration.

- Validations on form level (AJAX calls)

- validations on model level - if article importing is ON, and api key is blank, make sure to avoid errors

- Archive View
  - Use one-off module that installs View. Then delete. View stays. ?

- Children categories

- Default categories
